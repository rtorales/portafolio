# ContactCard

Icon + text contact row. Translucent on the dark contact band, bordered card on light. Renders as a link when `href` is set.

```jsx
<ContactCard icon={<MailIcon />} href="mailto:ricardotorales@outlook.com">
  ricardotorales@outlook.com
</ContactCard>
```
