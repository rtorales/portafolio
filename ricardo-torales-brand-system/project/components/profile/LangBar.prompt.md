# LangBar

Labelled proficiency meter — name, level label, and a navy→accent gradient fill.

```jsx
<LangBar name="Español" level="Nativo" value={100} />
<LangBar name="Inglés" level="Intermedio" value={60} />
```
